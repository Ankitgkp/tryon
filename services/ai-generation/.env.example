# AI Generation Service configuration
PORT=3005
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# OpenRouter API Key (required for real generation)
OPENROUTER_API_KEY=

# Set to "mock" to use the mock provider (no API key needed)
AI_PROVIDER_MODE=mock
